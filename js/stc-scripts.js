( function( $ ) {


	$(document).ready(function() {

    /*
    if( $('#stc-unsubscribe-wrapper').length > 0 ){
      message = $('#stc-unsubscribe-wrapper').clone();
      $('#stc-unsubscribe-wrapper').remove();
      $('body').prepend( message );
    }
    */

    });


} )( jQuery );